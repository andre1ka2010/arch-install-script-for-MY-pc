# arch-install-script-for-MY-pc
This script assumes that you use amd cpu and gpu, your drive is /dev/nvme0n1 and will re-partition it and format, install some apps but will not install desktop environment.
